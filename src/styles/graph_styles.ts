import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	chart: {
		borderRadius: 15,
	},
	title: {
		textAlign: "center",
		fontSize: 20,
		color: "#fff",
		fontWeight: 500,
		marginBottom: 10,
	},
});

export default styles;
