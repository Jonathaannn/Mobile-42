import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
	tabBarStyle: {
		position: "absolute",
		height: 60,
		bottom: 10,
		marginHorizontal: 10,
		overflow: "hidden",
		borderRadius: 15,
		borderTopWidth: 0,
		elevation: 0,
	},
	tabBarItemStyle: {
		margin: 5,
		borderRadius: 10,
		overflow: "hidden",
	},
});

export default styles;
